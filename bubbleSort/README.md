How It Works

The program defines two arrays: one for letters, one for numbers.

Calls bubble_sort with the appropriate comparator:

letters_in_order for letters

numbers_in_ascending_order for numbers

Prints the sorted arrays to the terminal.
